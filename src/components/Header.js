import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <div className="header">
      <span className="link">URL</span>
      <span className="shortener">Shortener</span>
    </div>
  );
};

export default Header;
